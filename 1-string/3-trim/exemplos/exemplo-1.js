// ✂️ EXEMPLO 1: Limpeza Básica
// ===========================

console.log("=== LIMPEZA BÁSICA ===\n");

// Removendo espaços desnecessários
const nomeComEspacos = "   João   ";
const nomeLimpo = nomeComEspacos.trim();

console.log(
  `Original: "${nomeComEspacos}" - Tamanho: ${nomeComEspacos.length}`
);
console.log(`Limpo: "${nomeLimpo}" - Tamanho: ${nomeLimpo.length}`);
