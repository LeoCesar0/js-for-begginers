// 📏 EXEMPLO 1: Contagem Básica
// =============================

console.log("=== CONTAGEM BÁSICA ===\n");

// Contando caracteres de diferentes strings
const nome = "Roberto";
const sobrenome = "Silva";
const nomeCompleto = "Roberto Silva";

console.log(`Nome: "${nome}" - Tamanho: ${nome.length}`);
console.log(`Sobrenome: "${sobrenome}" - Tamanho: ${sobrenome.length}`);
console.log(
  `Nome completo: "${nomeCompleto}" - Tamanho: ${nomeCompleto.length}`
);
