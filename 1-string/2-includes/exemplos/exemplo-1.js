// 🔍 EXEMPLO 1: Verificação Básica
// ===============================

console.log("=== VERIFICAÇÃO BÁSICA ===\n");

// Verificando se uma string contém texto
const frase = "JavaScript é incrível";
const temJavaScript = frase.includes("JavaScript");
const temPython = frase.includes("Python");

console.log(`Frase: ${frase}`);
console.log(`Contém 'JavaScript': ${temJavaScript}`);
console.log(`Contém 'Python': ${temPython}`);
